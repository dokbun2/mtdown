name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install customtkinter yt-dlp pyinstaller

      - name: Build macOS app
        run: |
          pyinstaller --onefile --windowed \
            --name "YouTube Downloader" \
            --add-data "downloader.py:." \
            app.py

      - name: Create DMG
        run: |
          mkdir -p dist/dmg
          cp -r "dist/YouTube Downloader.app" dist/dmg/
          hdiutil create -volname "YouTube Downloader" \
            -srcfolder dist/dmg \
            -ov -format UDZO \
            "dist/YouTube-Downloader-macOS.dmg"

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos-build
          path: dist/YouTube-Downloader-macOS.dmg

  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install customtkinter yt-dlp pyinstaller

      - name: Build Windows exe
        run: |
          pyinstaller --onefile --windowed `
            --name "YouTube Downloader" `
            --add-data "downloader.py;." `
            app.py

      - name: Rename output
        run: |
          mv "dist/YouTube Downloader.exe" "dist/YouTube-Downloader-Windows.exe"

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: dist/YouTube-Downloader-Windows.exe

  release:
    needs: [build-macos, build-windows]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/macos-build/YouTube-Downloader-macOS.dmg
            artifacts/windows-build/YouTube-Downloader-Windows.exe
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
